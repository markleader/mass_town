# Mock Backend Knowledge

- The mock backends are deterministic and primarily support tests and lightweight checked-in examples.
- Prefer mock-backed examples when validating outer-loop orchestration logic in the `default` environment.
